[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
dockerBuildArgs = ["VITE_CLERK_PUBLISHABLE_KEY"]

[deploy]
startCommand = "./start.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
